# Migration `watch-20200109154039`

This migration has been generated by Marvin ROGER at 1/9/2020, 3:40:39 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Flight" (
  "businessInitialPrice" INTEGER NOT NULL DEFAULT 0  ,
  "businessSeats" INTEGER NOT NULL DEFAULT 0  ,
  "date" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "destination" TEXT NOT NULL DEFAULT ''  ,
  "ecoInitialPrice" INTEGER NOT NULL DEFAULT 0  ,
  "ecoSeats" INTEGER NOT NULL DEFAULT 0  ,
  "id" TEXT NOT NULL   ,
  "origin" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Flight" ("businessInitialPrice","businessSeats","destination","ecoInitialPrice","ecoSeats","id","origin") SELECT "businessInitialPrice","businessSeats","destination","ecoInitialPrice","ecoSeats","id","origin" from "Flight"

DROP TABLE "quaint"."Flight";

ALTER TABLE "quaint"."new_Flight" RENAME TO "Flight";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;

PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Seat" (
  "class" TEXT NOT NULL DEFAULT 'ECO'  ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "flight" TEXT NOT NULL   REFERENCES "Flight"(id) ON DELETE RESTRICT,
  "id" TEXT NOT NULL   ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Seat" ("class","createdAt","flight","id","name","updatedAt") SELECT "class","createdAt","flight","id","name","updatedAt" from "Seat"

DROP TABLE "quaint"."Seat";

ALTER TABLE "quaint"."new_Seat" RENAME TO "Seat";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration watch-20200109153621..watch-20200109154039
--- datamodel.dml
+++ datamodel.dml
@@ -3,15 +3,16 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 model Flight {
   id    String  @default(cuid()) @id
   origin String
   destination String
+  date DateTime
   businessInitialPrice Int
   businessSeats Int
   ecoInitialPrice Int
```


